<h1>Modals</h1>

<h2>Modal creation</h2>

<p>Note: the following modal components add permanent modal markup to the DOM. So dynamic modal creation below for alternatives.</p>

<h3>Minimal markup</h3>
{{#bs-modal open=modal1 title="Simple Dialog" size="sm" closeAction=(action "closeModal1")}}
    Hi there
{{/bs-modal}}

<label>{{input type="checkbox" checked=modal1}} Open </label>

<h3>Explicit markup</h3>

{{#bs-modal open=modal2 header=false body=false footer=false submitAction=(action "submit") closeAction=(action "closeModal2")}}
    {{#bs-modal-header}}
        <h4 class="modal-title">Dialog</h4>
    {{/bs-modal-header}}
    {{#bs-modal-body}}Hi there{{/bs-modal-body}}
    {{bs-modal-footer closeTitle="Cancel" submitTitle="Ok"}}
{{/bs-modal}}

<label>{{input type="checkbox" checked=modal2}} Open </label>


<h2>Dynamic modal creation</h2>

{{#bs-button action=(action "addModal3")}}Open{{/bs-button}}

{{#if hasModal3}}
{{#bs-modal open=modal3 closeAction=(action "closeModal3") closedAction=(action "removeModal3") title="Dynamic Dialog"}}
    Hi there
{{/bs-modal}}
{{/if}}