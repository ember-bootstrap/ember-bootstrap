<h1>Forms</h1>

<p>Layout:</p>
{{#bs-button-group value=formLayout type="radio" onChange=(action (mut formLayout)) as |bg|}}
    {{#bg.button value="vertical"}}Vertical{{/bg.button}}
    {{#bg.button value="horizontal"}}Horizontal{{/bg.button}}
    {{#bg.button value="inline"}}Inline{{/bg.button}}
{{/bs-button-group}}


<h2>Automatically bound fields, automatic markup, with validation</h2>

{{#bs-form formLayout=formLayout model=this onSubmit=(action "submit") as |form|}}
    {{form.element controlType="email" label="Email" placeholder="Email" property="email" required=true}}
    {{form.element controlType="password" label="Password" placeholder="Password" property="password" required=true}}
    {{form.element controlType="checkbox" label="Remember me" property="rememberMe"}}
    {{form.element controlType="textarea" label="Comments" property="comments"}}
    {{bs-button defaultText="Submit" type="primary" buttonType="submit"}}
{{/bs-form}}

<h2>Manually bound fields, automatic markup, no validations</h2>

{{#bs-form formLayout=formLayout as |form|}}
    {{form.element controlType="email" label="Email" placeholder="Email" value=email}}
    {{form.element controlType="password" label="Password" placeholder="Password" value=password}}
    {{form.element controlType="checkbox" label="Remember me" value=rememberMe}}
    {{form.element controlType="textarea" label="Comments" value=comments}}
{{/bs-form}}

<h2>Custom Controls</h2>
{{#bs-form formLayout=formLayout model=this onSubmit=(action "submit") as |form|}}
    {{#form.element label="Email" placeholder="Email" property="email" as |el|}}
      <div class="input-group">
        {{bs-input id=el.id value=el.value}}
        <span class="input-group-addon">@example.com</span>
      </div>
    {{/form.element}}
    {{bs-button defaultText="Submit" type="primary" buttonType="submit"}}
{{/bs-form}}


<h2>Disabled Controls</h2>

{{#bs-form formLayout=formLayout as |form|}}
   {{form.element controlType="email" label="Email" placeholder="Email" value=email disabled=true}}
   {{form.element controlType="password" label="Password" placeholder="Password" value=password disabled=true}}
   {{form.element controlType="checkbox" label="Remember me" value=rememberMe disabled=true}}
   {{form.element controlType="textarea" label="Comments" value=comments disabled=true}}
{{/bs-form}}
