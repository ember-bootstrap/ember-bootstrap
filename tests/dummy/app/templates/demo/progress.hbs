<div class="bs-example">
  <!-- BEGIN-SNIPPET progress-demo -->
  <BsProgress as |p|>
    <P.bar value="50" />
  </BsProgress>
  <!-- END-SNIPPET -->
</div>
<div class="highlight">
  <CodeSnippet @name="progress-demo.hbs" />
</div>
<h2>
  Options
</h2>
<div class="bs-example">
  <BsForm @formLayout="horizontal" @model={{this}} as |form|>
    <Form.element @controlType="number" label="Value" @property="value" />
    <Form.element @controlType="number" label="Min" @property="minValue" />
    <Form.element @controlType="number" label="Max" @property="maxValue" />
    <Form.element
      @controlType="power-select"
      @options={{typeChoices}}
      label="Type"
      @property="type" as |el|
    >
      <El.control @searchEnabled />
    </Form.element>
    <Form.element
      @controlType="checkbox"
      label="Show label"
      @property="showLabel"
     />

    <Form.element
      @controlType="number"
      label="Round to digits"
      @property="roundDigits"
     />

    <Form.element @controlType="checkbox" label="Striped" @property="striped" />
    <Form.element
      @controlType="checkbox"
      @disabled={{notStriped}}
      label="Animate"
      @property="animate"
     />

  </BsForm>
  <hr />
  <!-- BEGIN-SNIPPET progress-options -->
  <BsProgress as |p|>
    <P.bar
      value={{value}}
      @minValue={{minValue}}
      @maxValue={{maxValue}}
      @showLabel={{showLabel}}
      @roundDigits={{roundDigits}}
      @type={{type}}
      @striped={{striped}}
      @animate={{animate}}
     />

  </BsProgress>
  <!-- END-SNIPPET -->
</div>
<div class="highlight">
  <CodeSnippet @name="progress-options.hbs" />
</div>
<h2>
  Stacked
</h2>
<div class="bs-example">
  <!-- BEGIN-SNIPPET progress-stacked -->
  <BsProgress as |p|>
    <P.bar
      value={{value}}
      @minValue={{minValue}}
      @maxValue={{maxValue}}
      @showLabel={{showLabel}}
      @roundDigits={{roundDigits}}
      @type={{type}}
      @striped={{striped}}
      @animate={{animate}}
     />

    <P.bar
      value="20"
      @minValue={{minValue}}
      @maxValue={{maxValue}}
      @showLabel={{showLabel}}
      @roundDigits={{roundDigits}}
      @type="warning"
      @striped={{striped}}
      @animate={{animate}}
     />

    <P.bar
      value="5"
      @minValue={{minValue}}
      @maxValue={{maxValue}}
      @showLabel={{showLabel}}
      @roundDigits={{roundDigits}}
      @type="danger"
      @striped={{striped}}
      @animate={{animate}}
     />

  </BsProgress>
  <!-- END-SNIPPET -->
</div>
<div class="highlight">
  <CodeSnippet @name="progress-stacked.hbs" />
</div>