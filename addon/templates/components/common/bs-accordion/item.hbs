{{#if hasBlockParams}}
  {{yield
    (hash
      title=(component titleComponent collapsed=collapsed disabled=disabled onClick=(action onClick value))
      body=(component bodyComponent collapsed=collapsed)
    )
  }}

{{else}}
  {{#component titleComponent collapsed=collapsed disabled=disabled onClick=(action onClick value)}}
    {{title}}
  {{/component}}
  {{#component bodyComponent collapsed=collapsed}}
    {{yield}}
  {{/component}}
{{/if}}
