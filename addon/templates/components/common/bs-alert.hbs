{{#unless this.hidden}}
  {{#if this.dismissible}}
    <button type="button" class="close" aria-label="Close" {{action "dismiss"}}>
      <span aria-hidden="true">&times;</span>
    </button>
  {{/if}}
  {{yield}}
{{/unless}}
