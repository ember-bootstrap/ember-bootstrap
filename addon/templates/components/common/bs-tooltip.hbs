{{#if inDom}}
  {{#bs-tooltip/element
    id=overlayId
    placement=_placement
    fade=fade
    showHelp=showHelp
    class=class
    renderInPlace=_renderInPlace
    target=triggerTargetElement
  }}
    {{#if hasBlock}}
      {{yield}}
    {{else}}
      {{title}}
    {{/if}}
  {{/bs-tooltip/element}}
{{/if}}
