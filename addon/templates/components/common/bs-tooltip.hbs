{{#if inDom}}
  {{#bs-tooltip/element
    id=overlayId
    placement=placement
    fade=fade
    showHelp=showHelp
    class=class
    renderInPlace=_renderInPlace
    target=triggerTargetElement
    autoPlacement=autoPlacement
    viewportElement=viewportElement
    viewportPadding=viewportPadding
  }}
    {{#if hasBlock}}
      {{yield}}
    {{else}}
      {{title}}
    {{/if}}
  {{/bs-tooltip/element}}
{{/if}}
