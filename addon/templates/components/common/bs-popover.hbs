{{#if inDom}}

    {{#ember-wormhole to="ember-bootstrap-modal-container" renderInPlace=_renderInPlace}}

        {{#bs-popover/element id=overlayId placement=_placement fade=fade in=in title=title class=class}}
            {{#if hasBlock}}
                {{yield}}
            {{else}}
                {{title}}
            {{/if}}
        {{/bs-popover/element}}

    {{/ember-wormhole}}

{{/if}}