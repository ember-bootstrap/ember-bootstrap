<div class={{if this.collapsed "collapsed" "expanded"}} ...attributes {{on "click" this.handleClick}}>
  <h4 class="panel-title">
    <a href="#" class={{if @disabled "disabled"}}>
      {{yield}}
    </a>
  </h4>
  
</div>