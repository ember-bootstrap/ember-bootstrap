{{#if hasBlock}}
    {{yield}}
{{else}}
    {{#if hasSubmitButton}}
        {{#bs-button type="default" action=(action onClose)}}{{closeTitle}}{{/bs-button}}
        {{#bs-button type="primary" action=(action onSubmit) disabled=submitDisabled}}{{submitTitle}}{{/bs-button}}
    {{else}}
        {{#bs-button type="primary" action=(action onClose)}}{{closeTitle}}{{/bs-button}}
    {{/if}}
{{/if}}