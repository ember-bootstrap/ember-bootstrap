<div
  class="accordion"
  ...attributes
>
  {{yield
    (hash
      item=(component (ensure-safe-component this.itemComponent) selected=this.isSelected onClick=this.doChange)
      change=this.doChange
    )
  }}
</div>
